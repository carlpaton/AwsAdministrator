@model List<WebApp.Models.DescribeNetworkAclsModel>

@for (int i = 0; i < Model.Count(); i++)
{
    <dl class="row">
        <dt class="col-sm-2">
            <label asp-for="@Model[i].NetworkAclsId"></label>
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model[i].NetworkAclsId)
        </dd>
    </dl>
}