@model List<AwsAdmin.WebApp.Models.DescribeInternetGatewayModel>

@for (int i = 0; i < Model.Count(); i++)
{
    <dl class="row">
        <dt class="col-sm-2">
            <label asp-for="@Model[i].InternetGatewayId"></label>
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model[i].InternetGatewayId)
        </dd>
    </dl>
}