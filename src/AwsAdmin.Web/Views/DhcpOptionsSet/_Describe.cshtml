@model List<AwsAdmin.WebApp.Models.DescribeDhcpOptionsSetModel>

@for (int i = 0; i < Model.Count(); i++)
{
    <dl class="row">
        <dt class="col-sm-2">
            <label asp-for="@Model[i].DhcpOptionsSetId"></label>
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model[i].DhcpOptionsSetId)
        </dd>
    </dl>
}